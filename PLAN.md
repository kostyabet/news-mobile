# PLAN.md — Vazon News: план реализации недостающего функционала

## Анализ покрытия БД

### Реализовано (Backend + Mobile)

| Сущность БД | Backend API | Mobile |
|---|---|---|
| **User** (auth) | `POST /users`, `POST /users/login`, `POST /users/refresh` | login, register экраны |
| **UserInfo** (profile) | `GET/PATCH/DELETE /profile/:id`, `POST /profile/:id/avatar` | profile экран, редактирование |
| **UserRole** | `GET /user-roles` | dropdown при регистрации |
| **Article** | CRUD + `/my` + upload image | лента, поиск, детали, создание/редактирование |

### Не реализовано

| Сущность БД | Backend | Mobile | Описание |
|---|---|---|---|
| **Comment** | нет | нет | Комментарии к статьям (с вложенностью через `c_parent`) |
| **Reaction** | нет | нет | Реакции на статьи (лайк/дизлайк и т.д.) |
| **Subscription** | нет | нет | Подписка на авторов |
| **Notification** | нет | нет | Уведомления пользователей (с типами и статусами) |
| **Settings** | нет | частично (локально) | Серверные настройки (тема, язык, email/push уведомления) |
| **Category** | нет | нет | Категории статей |
| **Tag** | нет | нет | Теги статей |
| **ArticleCategory** | нет | нет | Привязка статей к категориям (M2M) |
| **ArticleTag** | нет | нет | Привязка статей к тегам (M2M) |
| **ArticleStatus** | используется | фильтров нет | Статусы статей (draft/published/etc) |
| **Language** | нет | нет | Язык статьи (серверная привязка) |

---

## Фаза 1 — Категории и Теги (фундамент контента)

### Backend

- [x] 1.1. CRUD для `Category` — `GET /categories`, `POST`, `PUT /:id`, `DELETE /:id`
           Доступна только по токену и пользователю с ролью Admin
- [x] 1.2. CRUD для `Tag` — `GET /tags`, `POST`, `PUT /:id`, `DELETE /:id`
           Доступна только по токену и пользователю с ролью Admin
- [x] 1.3. Эндпоинты привязки: `POST /articles/:id/categories`, `POST /articles/:id/tags`
           Доступна по токену и добавить может только создатель статьи
- [x] 1.4. Фильтрация статей по категории и тегу (`GET /articles?category=X&tag=Y`)
           Доступна по токену.
Для каждого запроса нужно соблюдать требования по DTO и документации под Swagger.

### Mobile

- [x] 1.5. Выбор категорий и тегов при создании/редактировании статьи
           При создании или редактировании статьи в модальное окно добавить внизу в виде space-between два блока. Слева теги справа категории и реализовать функционал их добавления.
- [x] 1.6. Фильтр по категориям на экране ленты и поиска
           Добавить в фильтры и сортировки теги и категории
- [x] 1.7. Отображение тегов и категорий на экране статьи
           В блоке рендера статьи. справа от реакции отрисовать pill категорий и тегов. Категории и теги зрительно отличаются.

---

## Фаза 2 — Реакции

### Backend

- [x] 2.1. `POST /articles/:id/reactions` — поставить/изменить реакцию (Работает только с токеном)
- [x] 2.2. `DELETE /articles/:id/reactions` — убрать реакцию (работает только с токеном)
- [x] 2.3. `GET /articles/:id/reactions` — счётчики реакций + реакция текущего юзера (работает только с токеном)
- [x] 2.4. Seed `ReactionType` (like, dislike и т.д.) (работает без токена)

### Mobile

- [x] 2.5. Кнопки реакций на экране статьи (лайк/дизлайк)
      Нужно чтобы на странице article кнопка shared стала справа ну и слева блок с двумя квадратными кнопками liki/dislike в виде эмоджи.
- [x] 2.6. Отображение счётчиков реакций в карточках ленты
      Рядом со стикерами лайков и дизлайков их кол-во. На главной в каждой карточке в правом нижнем углу тоже должен быть счётчик.

---

## Фаза 3 — Комментарии

### Backend

- [x] 3.1. `GET /articles/:id/comments` — дерево комментариев
- [x] 3.2. `POST /articles/:id/comments` — добавить комментарий (с опциональным `parent`)
- [x] 3.3. `PUT /comments/:id` — редактировать свой комментарий
- [x] 3.4. `DELETE /comments/:id` — удалить свой комментарий

### Mobile

- [x] 3.5. Секция комментариев на экране статьи (вложенные/древовидные)
- [x] 3.6. Форма добавления комментария
- [x] 3.7. Ответ на комментарий (reply)
- [x] 3.8. Редактирование/удаление своих комментариев

---

## Фаза 4 — Подписки

### Backend

- [x] 4.1. `POST /subscriptions/:authorId` — подписаться
- [x] 4.2. `DELETE /subscriptions/:authorId` — отписаться
- [x] 4.3. `GET /subscriptions/my` — мои подписки
- [x] 4.4. `GET /subscriptions/:authorId/subscribers` — подписчики автора
- [x] 4.5. Seed `SubscriptionStatus`
Статусы подписок (id, status):
  1,unsub
  2,sub

### Mobile

- [x] 4.6. Кнопка "Подписаться" на профиле автора
- [x] 4.7. Список подписок на экране профиля
- [x] 4.8. Лента "от подписок" (фильтр статей по подписанным авторам)
- [x] 4.9. В профиле пользователя нужно отображать количество подписчиков и количество подписок пользователя.

---

## Фаза 5 — Уведомления

### Backend

- [ ] 5.1. `GET /notifications` — список уведомлений текущего юзера
- [ ] 5.2. `PATCH /notifications/:id` — отметить как прочитанное
- [ ] 5.3. `PATCH /notifications/read-all` — прочитать все
- [ ] 5.4. Автоматическое создание уведомлений при: новом комментарии, реакции, новой статье от подписанного автора
- [ ] 5.5. Seed `NotificationType`, `NotificationStatus`

### Mobile

- [ ] 5.6. Экран/модал уведомлений (badge на табе или в шапке)
- [ ] 5.7. Список уведомлений с переходом к статье/комментарию
- [ ] 5.8. Отметка прочитанных

---

## Фаза 6 — Серверные настройки

### Backend

- [x] 6.1. `GET /settings` — получить настройки текущего юзера
- [x] 6.2. `PUT /settings` — обновить настройки (тема, язык, push/email уведомления)
- [x] 6.3. Seed `Theme`, `Language`
  theme (id, theme):
   1,system
   2,dark
   3,light

  language (id, lang):
   1,RU
   2,EN


### Mobile

- [x] 6.4. Синхронизация настроек с сервером (сейчас всё локально в AsyncStorage)
- [x] 6.5. Привязка push/email уведомлений к серверным настройкам

---

## Фаза 7 — Статусы статей и языки

### Backend

- [ ] 7.1. `GET /article-statuses` — список статусов
- [ ] 7.2. Фильтрация `GET /articles?status=X&language=Y`
- [ ] 7.3. `GET /languages` — список языков

### Mobile

- [ ] 7.4. Выбор языка статьи при создании
- [ ] 7.5. Фильтр по языку и статусу в ленте

# Fixes

## ~~Scroll update on News page and in Profile~~ DONE
Необходимо реализовать функционал обновления главной странице и на странице профиля(личного и по id) по скролу вверх.
Нужно сделать так, что если поле поиска уже активно (оно также открывается при скроле вверх) и пользователь
всё равно ещё скролит вверх то там появляется иконка загрузки и мы обновляем данные на главной страниец и отображаем их пользователю.
Необходимо оформить всё красиво и в соответствии со стилистикой приложения. При протягивании вверх пользователь должен понимать, что он обновит данные
Когда пользователь отпустит скролл нужен какой-то индикатор, что данные загружаются. Индикатор пропадёт по окончанию загрузки.

## ~~Main page skeleton~~ DONE
Рендеринг основной страницы изменился, позиционирование элементов и нужно изменить skeleton чтобы он соответствовал текущему виду списка артиклов.

## ~~Article loading~~ DONE
Когда пользователь нажимает на артикл, то на бек улетает запрос, нужно, чтобы до того как мы точно убедимся, что артикла нет, то пользователю показывался какой-то loading.
Сейчас, как я понимаю, пока артикл не загружен то там где-то null или пусто и он пишет текст, что article не найден, но на самом деле может просто идёт запрос на бек с получением данных из кеша или ещё что-то.

## ~~Cache fixes~~ DONE
Необходимо реализовать кеширования всей статики (фото)

## ~~Camera snap~~ DONE
Необходимо добавить возможность сделать фото при создании проекта. Данный функицонал уже есть при работе с аватаром в профиле, по нажатию выподает меню выбора, выбирать из галереи или снять камерой.

## ~~Lazy data loading~~ DONE
Нужно чтобы данные на основной странице загружались по алгоритму page limit. Нужно реализовать его на бекенде. Также он нужен для получение статей автора и там тоже реализовать этот алгоритм. Нужно по дефолту загружать ~ 10 статей, остальные когда пользователь начинает скролить вниз.

## ~~Data ranging~~ DONE
Нужно реализовать алгоритм получения самых популярных статей. Нужно чтобы на главной первыми отображались, получали мы самые популряные статьи.
Тоже самое должно быть в профиле, сначала самые популярные. Возможно, для этого нужно реализовать в бд что-то типо View (представления) чтобы всё было.

## ~~Comments~~ DONE
Нужно добавить возможность оставлять реакции на комментарии.
Нужно чтобы по нажатию на профиль или на тег в коментарии открывался профиль этого пользователя.

## Profile
По нажатию на кнопку сохранить режим редактирования в профиле должен спадать. Также есть баг когда обновляешь информацию о пользователя, то по нажатию на Сохранить ничего не происходит, запрос на бек уходит, но в бд у пользователя не появляется запись UserInfo и соответственно информация не сохраняется.

## Auth
### SignIn
Нужно добавить кнопку gmail для входа по gmail
### SignUp
Нужно аналогично входу добавить через ту же кнопку возможность регистрации по gmail.
Почта которую пользователь указывает при регистрации по gmail пробрасывается в поле email в UserInfo.
После того, как пользователь зарегистрировался, его не перебрасывает на вход а сразу входит, ну то есть сразу запрос на signIn отправляется и пользователя перебрасывает на страницу о профиле.
Так как это новый профиль то у нас нет информации о first name last name email. Если вход через gmail то email у нас есть а first name last name можем взять через почту, если есть такая возможность.